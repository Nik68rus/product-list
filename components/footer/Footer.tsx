import Container from '../container/Container';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <Container>2023</Container>
    </footer>
  );
};

export default Footer;
